<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lesson8.android.extratask1" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PhotoGalleryActivity"
            android:label="@string/title_activity_photo_gallery" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PhotoViewActivity"
            android:label="@string/title_activity_photo_view"
            android:parentActivityName=".PhotoGalleryActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".PhotoGalleryActivity" />
        </activity>
        <provider android:name="lesson8.android.extratask1.PhotoProvider"
                  android:authorities="lesson8.android.extratask1"/>
    </application>

</manifest>
